# General commands

## `PUSH` /api/danger/mockdb

if the db is empty, generates some fake example data on the "main" database

## `GET ` /api/isdbempty

```ts
type Returns = {
    empty: boolean;
};
```
